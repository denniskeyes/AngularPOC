import { HeaderScrollOpacityDirective } from './header-scroll-opacity.directive';

describe('HeaderScrollOpacityDirective', () => {
  it('should create an instance', () => {
    const directive = new HeaderScrollOpacityDirective();
    expect(directive).toBeTruthy();
  });
});
